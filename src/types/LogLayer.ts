import type {
  ILogLayer,
  LogLayerPlugin,
  LogLayerTransport,
  LogLayerTransportParams,
  LogLevel,
  LogLevelType,
  MessageDataType,
  RawLogEntry,
} from '@loglayer/shared'

export type {
  // might not be necessary...
  ILogLayer,
  LogLayerPlugin,
  LogLayerTransport,
  LogLayerTransportParams,
  LogLevel,
  LogLevelType,
  MessageDataType,
  RawLogEntry,
}
